-- ============================================
-- LaporanMRFH - Seed Data
-- ============================================
-- NOTE: Run this AFTER creating the admin user via Supabase Auth UI.
-- Replace 'AUTH_USER_ID' with the actual auth.uid() from Supabase.

-- Example: Create admin user in public.users (after creating via Auth)
-- INSERT INTO public.users (username, name, role, auth_id)
-- VALUES ('admin', 'Administrator', 'admin', 'AUTH_USER_ID_HERE');
